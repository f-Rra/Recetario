@model IngredienteFormViewModel
@{
    ViewData["Title"] = "Editar ingrediente";
}

<div class="row justify-content-center">
    <div class="col-md-6 col-lg-5">
        <div class="card">
            <div class="card-body">
                <form asp-action="Editar" method="post">
                    <input type="hidden" asp-for="IdIngrediente" />
                    <partial name="_Form" model="Model" />
                    <div class="row">
                        <div class="col mb-4">
                            <label asp-for="StockActual" class="form-label"></label>
                            <input asp-for="StockActual" class="form-control" readonly />
                            <div class="form-text">
                                Solo se mueve por <a asp-controller="Stock" asp-action="Index">movimientos</a>.
                            </div>
                        </div>
                        <div class="col mb-4">
                            <label asp-for="StockMinimo" class="form-label"></label>
                            <input asp-for="StockMinimo" class="form-control" />
                            <span asp-validation-for="StockMinimo" class="text-danger"></span>
                        </div>
                    </div>
                    <button type="submit" class="btn btn-primary">Guardar cambios</button>
                    <a asp-action="Index" class="btn btn-outline-secondary">Cancelar</a>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
