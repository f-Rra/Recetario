@{
    ViewData["Title"] = "Dashboard";
}

<div class="card">
    <div class="card-body text-center py-5">
        <i class="bi bi-egg-fried" style="font-size: 40px; color: var(--sx-celeste);"></i>
        <h2 class="h5 mt-3 mb-1">¡Bienvenido!</h2>
        <p class="text-muted mb-0">El panel de cocina (comandas del día y consulta de recetas) llega en la fase 3.</p>
    </div>
</div>
